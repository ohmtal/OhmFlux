# BitmapFont
A small gui tool to create bitmap fonts which can be used with ohmFlux or
every other softwate which is able to load bitmaps.
    
## written with Lazarus/Pascal 

To compile you need Lazarus, once the gui is setup you can use:

	lazbuild BitmapFont.lpi
	
to compile it. 
